<!doctype html>
<html lang="zh-CN">
<head>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width,initial-scale=1,viewport-fit=cover">
<meta name="description" content="2026年9月25日至10月2日青甘大环线自驾路书：西宁、青海湖、茶卡、察尔汗、大柴旦、黑独山、敦煌、嘉峪关、张掖、祁连、门源。">
<title>青甘大环线 · 2026.09.25–10.02 自驾路书</title>
<link rel="preconnect" href="https://unpkg.com">
<link rel="stylesheet" href="https://unpkg.com/leaflet@1.9.4/dist/leaflet.css">
<style>
:root {
  --bg:#f4f1e9; --paper:#fffdf8; --ink:#1f2a2a; --muted:#66706c; --line:#dfddd3;
  --accent:#9f5b35; --accent2:#2f6963; --sand:#d7b27a; --danger:#a43d35;
  --shadow:0 16px 50px rgba(39,48,45,.10); --radius:22px;
}
*{box-sizing:border-box} html{scroll-behavior:smooth}
body{margin:0;font-family:Inter,"PingFang SC","Microsoft YaHei",system-ui,-apple-system,sans-serif;background:
radial-gradient(circle at 10% 0%,rgba(214,178,122,.28),transparent 28rem),
radial-gradient(circle at 90% 12%,rgba(47,105,99,.12),transparent 24rem),var(--bg);color:var(--ink);line-height:1.7}
a{color:var(--accent2)} .wrap{width:min(1160px,calc(100% - 28px));margin:auto}
.hero{padding:54px 0 34px;position:relative;overflow:hidden}
.hero:after{content:"";position:absolute;right:-5%;bottom:-90px;width:520px;height:260px;border-radius:50%;background:linear-gradient(135deg,rgba(159,91,53,.18),rgba(47,105,99,.08));transform:rotate(-12deg);z-index:-1}
.kicker{font-size:13px;letter-spacing:.18em;text-transform:uppercase;color:var(--accent2);font-weight:800}
h1{font-size:clamp(36px,7vw,78px);line-height:1.04;letter-spacing:-.04em;margin:10px 0 18px;max-width:980px}
.lead{font-size:clamp(17px,2.4vw,23px);max-width:860px;color:#4c5754}
.hero-grid{display:grid;grid-template-columns:1.4fr .8fr;gap:20px;margin-top:30px}
.hero-card,.panel,.day-card{background:rgba(255,253,248,.92);border:1px solid rgba(105,91,69,.14);box-shadow:var(--shadow);border-radius:var(--radius)}
.hero-card{padding:22px} .hero-card strong{font-size:28px;display:block} .hero-card small{color:var(--muted)}
.key-alert{background:#2c3130;color:white;padding:22px;border-radius:var(--radius);box-shadow:var(--shadow)}
.key-alert b{color:#ffd08a} .key-alert p{margin:6px 0 0;color:#e7ecea}
.actions{display:flex;flex-wrap:wrap;gap:10px;margin-top:22px}
.btn{border:0;border-radius:999px;padding:12px 18px;font-weight:800;cursor:pointer;background:var(--ink);color:white;font-size:14px}
.btn.secondary{background:var(--paper);color:var(--ink);border:1px solid var(--line)}
.sticky{position:sticky;top:0;z-index:50;backdrop-filter:blur(14px);background:rgba(244,241,233,.88);border-top:1px solid rgba(0,0,0,.04);border-bottom:1px solid rgba(0,0,0,.08)}
.nav{display:flex;gap:16px;overflow-x:auto;padding:10px 0;white-space:nowrap}
.nav a{text-decoration:none;color:var(--ink);font-size:14px;font-weight:750;padding:7px 10px;border-radius:10px}
.nav a:hover{background:#fff}
section{padding:34px 0} h2{font-size:clamp(27px,4.5vw,44px);letter-spacing:-.035em;margin:0 0 8px}
.section-lead{color:var(--muted);max-width:850px;margin:0 0 22px}
.grid-3{display:grid;grid-template-columns:repeat(3,1fr);gap:14px}
.callout{padding:20px;border-radius:18px;border:1px solid var(--line);background:#fffdf8}
.callout h4{margin:0 0 8px;font-size:18px} .callout p{margin:0;color:var(--muted)}
.callout.danger{border-color:#d9a7a2;background:#fff7f6} .callout.good{border-color:#b9d6cf;background:#f6fffc}
.compare{display:grid;grid-template-columns:1fr 1fr;gap:16px}
.panel{padding:22px} .panel h3{margin-top:0}
.route-strip{display:flex;flex-wrap:wrap;gap:8px;margin-top:12px}
.route-strip span{font-size:13px;background:#f0ece1;padding:7px 10px;border-radius:999px}
.route-strip .blocked{background:#ffe1de;color:#8f2b24;text-decoration:line-through}
.route-strip .new{background:#dff3ec;color:#235c54}
#map{height:480px;border-radius:22px;overflow:hidden;border:1px solid var(--line);background:#e8e4da}
.map-fallback{display:none;padding:20px;background:#fff;border-radius:18px}
.map-note{color:var(--muted);font-size:13px;margin-top:8px}
.day-list{display:grid;gap:18px}
.day-card{padding:24px}
.day-head{display:flex;justify-content:space-between;gap:20px;align-items:flex-start}
.eyebrow{font-size:13px;color:var(--accent2);font-weight:850;letter-spacing:.08em}
.day-head h3{font-size:clamp(23px,3vw,32px);margin:4px 0 8px;line-height:1.25}
.tags{display:flex;flex-wrap:wrap;gap:7px} .pill{font-size:12px;padding:5px 9px;border-radius:999px;background:#f0ece1;color:#5b5b52}
.risk{padding:8px 12px;border-radius:999px;font-size:12px;font-weight:900;white-space:nowrap}
.risk.ok{background:#dff3ec;color:#235c54} .risk.mid{background:#fff0bf;color:#765d00} .risk.high{background:#ffe0c9;color:#8a4314} .risk.extreme{background:#ffd8d5;color:#912b24}
.metrics{display:grid;grid-template-columns:repeat(4,1fr);gap:10px;margin:18px 0}
.metrics div{background:#f6f3ec;border-radius:14px;padding:12px}
.metrics b{display:block;font-size:15px} .metrics span{font-size:12px;color:var(--muted)}
.timeline{border-top:1px dashed var(--line);padding-top:10px}
.step{display:grid;grid-template-columns:120px 1fr;gap:14px;padding:10px 0;border-bottom:1px dashed #ebe7de}
.step:last-child{border-bottom:0} .time{font-weight:850;color:var(--accent)} .what{color:#3e4946}
.pro-tip{margin-top:14px;background:#eef5f2;border-left:4px solid var(--accent2);padding:14px 16px;border-radius:12px}
.table-wrap{overflow:auto;border-radius:18px;border:1px solid var(--line);background:#fff}
table{border-collapse:collapse;width:100%;min-width:840px} th,td{padding:13px 14px;border-bottom:1px solid #eee9df;text-align:left;vertical-align:top;font-size:14px}
th{background:#f0ece1;position:sticky;top:0} tr:last-child td{border-bottom:0}
.check-grid{display:grid;grid-template-columns:repeat(2,1fr);gap:10px}
.check{display:flex;gap:10px;align-items:flex-start;background:#fff;border:1px solid var(--line);border-radius:14px;padding:12px}
.check input{margin-top:5px;transform:scale(1.15)}
.keyword-grid{display:flex;flex-wrap:wrap;gap:9px}
.keyword{border:1px solid var(--line);background:#fff;border-radius:999px;padding:9px 12px;cursor:pointer;color:var(--ink)}
.keyword span{color:var(--accent2);font-size:12px;font-weight:800;margin-left:5px}
.budget{display:grid;grid-template-columns:1fr 1fr;gap:18px}
.calc-grid{display:grid;grid-template-columns:repeat(2,1fr);gap:10px}
label{font-size:12px;color:var(--muted)} input[type=number]{width:100%;margin-top:4px;border:1px solid var(--line);border-radius:12px;padding:10px;background:#fff;font:inherit}
.total{font-size:42px;font-weight:900;letter-spacing:-.04em;color:var(--accent2)} .muted{color:var(--muted)}
.sources li{margin:7px 0} .sources a{word-break:break-all}
.footer{padding:32px 0 54px;color:var(--muted);font-size:13px}
.toast{position:fixed;left:50%;bottom:24px;transform:translateX(-50%) translateY(30px);background:#222;color:#fff;padding:10px 14px;border-radius:999px;opacity:0;pointer-events:none;transition:.2s;z-index:99}
.toast.show{opacity:1;transform:translateX(-50%) translateY(0)}
@media(max-width:800px){
  .hero-grid,.compare,.budget,.grid-3{grid-template-columns:1fr}
  .metrics{grid-template-columns:repeat(2,1fr)} .day-head{display:block} .risk{display:inline-block;margin-top:10px}
  .step{grid-template-columns:92px 1fr} .check-grid{grid-template-columns:1fr} #map{height:420px}
}
@media print{
  .sticky,.actions,.btn,.toast{display:none!important} body{background:#fff} .hero-card,.panel,.day-card{box-shadow:none;break-inside:avoid} section{padding:18px 0} #map{height:360px}
}

/* Mobile browsing enhancements only; no content removed */
@media (max-width: 800px) {
  body { -webkit-text-size-adjust: 100%; }
  .wrap { width: min(100% - 18px, 1160px); }
  .hero { padding: 34px 0 24px; }
  h1 { font-size: clamp(34px, 12vw, 54px); line-height: 1.02; }
  .lead { font-size: 16px; line-height: 1.75; }
  .hero-card, .key-alert, .panel, .day-card { border-radius: 18px; }
  .hero-card, .key-alert, .panel, .day-card { padding: 18px; }
  .actions { display: 